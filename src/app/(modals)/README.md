# (modals) route group

Screens presented on top of the app: settings, game-over summary, game invites,
purchase/store sheets, etc.

Once screens exist here, register them in the root `_layout.tsx` Stack with
`presentation: 'modal'` so they slide up over whatever's underneath instead of
replacing it.

Nothing lives here yet.
